---
phase: 01-close-in-progress-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css
  # Possibly home-page-locked files IF the icon-glyph fix turns out NOT to be the root cause:
  # - wp-content/themes/sandiegoweddingdirectory/front-page.php           (LOCKED — scoped unlock active for P1-FIX-02 only)
  # - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/home.css (LOCKED — scoped unlock active for P1-FIX-02 only)
  # - wp-content/themes/sandiegoweddingdirectory/assets/js/app.js
autonomous: false
requirements: [P1-FIX-02]
must_haves:
  truths:
    - "Clicking the home-page category-search filter (the 'Search by category' control) opens a dropdown panel showing selectable vendor-category options."
    - "Clicking a category option in the dropdown writes the term ID to the hidden input `cat_id` and closes the panel."
    - "Selecting a category passes the choice through to the downstream search submission."
    - "The icon-glyph hypothesis is investigated FIRST (devtools + grep) and a documented root cause is recorded before any code change is committed."
    - "Edits stay within the home-page scoped-unlock scope (D-19) — only the dropdown bug is touched; no other home-page changes."
    - "If the fix lives in `assets/library/sdwd-icons/style.css` (the most-likely path per PATTERNS.md), `front-page.php` and `home.css` are NOT touched (the home-page lock window is not exercised at all)."
  artifacts:
    - path: "wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css"
      provides: "Defines `icon-chevron-down` glyph (or aliases it to an existing chevron-down glyph) — most-likely root cause fix"
      contains: "icon-chevron-down"
  key_links:
    - from: "wp-content/themes/sandiegoweddingdirectory/front-page.php"
      to: "wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css"
      via: "CSS class `icon-chevron-down` referenced 4× in front-page.php markup; must resolve to a defined glyph in sdwd-icons/style.css"
      pattern: "icon-chevron-down"
    - from: "wp-content/themes/sandiegoweddingdirectory/front-page.php (dropdown trigger button)"
      to: "wp-content/themes/sandiegoweddingdirectory/assets/js/app.js (lines 184-242 dropdown handler)"
      via: "click event on .hero__dropdown-trigger toggles aria-expanded and panel.hidden"
      pattern: "hero__dropdown-trigger"
---

<objective>
Fix the home-page category-search mega-menu dropdown (P1-FIX-02). Per `PROJECT.md` §1: "Search doesn't work. Searching by category is supposed to drop down a mega menu. We had this working in some prior version." The category filter on the home-page search bar currently does nothing when clicked.

Per the pattern-mapper investigation (PATTERNS.md §front-page.php + §"Pattern guidance for the executor"): the dropdown markup AND the JS handler ALREADY EXIST and appear correct. The most likely root cause is a missing icon class — `front-page.php` references `icon-chevron-down` 4× in the dropdown trigger markup but this glyph is **not defined** in `assets/library/sdwd-icons/style.css` (only `icon-chevron-left` and `icon-angle-down` exist). The clickable area appears broken because the visual cue isn't rendering and/or there's a related CSS/scope issue.

**This plan is investigation-driven, not pre-committed.** Lead with hypothesis verification (devtools + grep), then apply the smallest possible fix. Per D-19, the home-page scoped unlock is active for THIS bug only — `front-page.php` and `home.css` may be edited only if the investigation confirms the fix requires it. The most-likely fix lives in `sdwd-icons/style.css` (NOT a locked file), so the lock might never need to be exercised.

Per D-23: NO changes to shared tokens (`foundation.css :root`), `components.css`, or `layout.css` under this scope. If a new token appears necessary, escalate.

Output: a working home-page category-search dropdown (clicking the trigger opens a panel with selectable vendor categories; selecting one closes the panel and persists the choice via the existing hidden `cat_id` input).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/CLAUDE-GSD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-close-in-progress-cleanup/01-CONTEXT.md
@.planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Existing markup, JS handler, and icon-font state. From PATTERNS.md §front-page.php + §app.js dropdown handler. -->

Dropdown markup ALREADY EXISTS (front-page.php:202-227 — the vendor-category dropdown):
  <div class="hero__field hero__field--type hero__field--dropdown">
    <span class="hero__field-icon icon-search" aria-hidden="true"></span>
    <button class="hero__dropdown-trigger" type="button" aria-expanded="false" aria-haspopup="true" data-dropdown="vendor-category">
      <span class="hero__dropdown-text">Search by category</span>
      <span class="hero__dropdown-arrow icon-chevron-down" aria-hidden="true"></span>   <-- icon-chevron-down NOT DEFINED in sdwd-icons
    </button>
    <input type="hidden" name="cat_id" value="">
    <div class="hero__dropdown-panel" data-panel="vendor-category" hidden>
      <div class="hero__dropdown-grid">
        <?php // get_terms( vendor-category ) loop, each as <button class="hero__dropdown-item" data-value="..."> ?>
      </div>
    </div>
  </div>

JS handler ALREADY EXISTS (app.js:184-242 — appears correct, do not rewrite):
  document.querySelectorAll('.hero__dropdown-trigger').forEach(trigger => {
    const field = trigger.closest('.hero__field');
    if (!field) return;
    const panel = field.querySelector('.hero__dropdown-panel');
    const hiddenInput = field.querySelector('input[type="hidden"]');
    const textEl = trigger.querySelector('.hero__dropdown-text');
    if (!panel) return;
    trigger.addEventListener('click', () => {
      const isOpen = trigger.getAttribute('aria-expanded') === 'true';
      // close other open panels
      // toggle this one: aria-expanded + panel.hidden
    });
    panel.querySelectorAll('.hero__dropdown-item').forEach(item => {
      item.addEventListener('click', () => {
        // active state, write hiddenInput.value, update trigger text, close panel
      });
    });
  });
  // outside-click close handler: app.js:234-242

Icon font state (verified at 2026-04-23 via grep — PATTERNS.md §front-page.php fix-hypothesis #1):
  assets/library/sdwd-icons/style.css defines:
    .icon-chevron-left
    .icon-angle-down       <-- this glyph LOOKS like a chevron-down; can be aliased
  assets/library/sdwd-icons/style.css does NOT define:
    .icon-chevron-down     <-- referenced 4× in front-page.php markup; the missing glyph

CSS dropdown panel rule (home.css:308-321 — unmodified, listed for reference):
  .hero__dropdown-panel {
    position: absolute;
    top: 100%;
    left: -16px;
    right: -16px;
    z-index: 100;
  }
  .hero__dropdown-panel[hidden] { display: none; }

Home-page scoped unlock (per CLAUDE.md + D-19):
  Editable for THIS bug only:
    - front-page.php
    - assets/css/pages/home.css
  Do NOT edit any other home-page-related file outside this scope.
  Lock re-engages on P1-FIX-02 verification complete.

Per D-20: do NOT touch inc/sd-mega-navwalker.php — that serves the top-nav mega-menu, NOT the home-page search-bar dropdown.

Per D-23: do NOT add new tokens to foundation.css, do NOT modify components.css or layout.css under this scope.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Hypothesis verification — confirm root cause BEFORE any code change</name>
  <files>(no files modified — investigation only)</files>
  <read_first>
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§front-page.php "Investigation summary" — three hypotheses ranked by likelihood)
    - wp-content/themes/sandiegoweddingdirectory/front-page.php (lines 159-247 — dropdown markup; check for `icon-chevron-down` references)
    - wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css (verify which icon-* classes are defined; specifically check whether `icon-chevron-down` is present)
    - wp-content/themes/sandiegoweddingdirectory/assets/js/app.js (lines 184-242 — confirm the dropdown handler is wired and reads `data-dropdown` etc.)
    - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/home.css (lines 261-357 — `.hero__dropdown-*` rules; check `.hero__dropdown-panel[hidden]` rule + parent containers for unexpected `overflow: hidden`)
  </read_first>
  <what-built>
    Investigation, not code. The pattern mapper's three hypotheses (PATTERNS.md):
      1. Missing icon class — `icon-chevron-down` referenced 4× in front-page.php but NOT defined in sdwd-icons/style.css. (Most likely.)
      2. CSS clipping / z-index — parent of `.hero__field--dropdown` has `overflow: hidden` somewhere upstream and panel can't escape. (Less likely.)
      3. CSS `[hidden]` overrides — a `display: block` rule overrides `[hidden] { display: none }`. (Unlikely; theme bans `!important`.)

    Run these mechanical checks and record the findings in `.planning/phases/01-close-in-progress-cleanup/01-03-INVESTIGATION.md` (a temporary scratchpad you create at the start of execution; the SUMMARY at end-of-plan will record final root cause + fix path).

    Mechanical checks (run from repo root):
    1. `grep -n "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css` — expect `0` matches if hypothesis #1 is correct
    2. `grep -nE "icon-(chevron-down|angle-down|caret-down|arrow-down)" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css` — confirm which down-pointing chevron-like glyphs DO exist
    3. `grep -n "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/front-page.php` — confirm count of references in markup (expect 4 per PATTERNS.md)
    4. `grep -nE "overflow:\s*hidden" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/home.css` — note any rules that might clip the panel
    5. Open `http://localhost:8080/` in a browser; open devtools Console; click the category-search trigger; record:
       - Does any JS error fire?
       - Does the trigger's `aria-expanded` flip to `"true"`?
       - Does the panel's `hidden` attribute disappear?
       - Is the panel painted (devtools Elements + Computed styles — what is `display`, `visibility`, `opacity`, `z-index`, `position`, parent `overflow`)?

    Record findings in 01-03-INVESTIGATION.md with this template:
      ## Hypothesis 1 (icon-chevron-down missing)
      - sdwd-icons/style.css contains `icon-chevron-down`: [yes/no]
      - sdwd-icons/style.css contains `icon-angle-down`: [yes/no]
      - sdwd-icons/style.css contains `icon-chevron-left`: [yes/no]
      - front-page.php icon-chevron-down references: [count]
      - **Verdict:** [confirmed / disproved]

      ## Hypothesis 2 (CSS clipping / z-index)
      - Parent containers with overflow: hidden: [list]
      - Panel computed `display` when trigger clicked: [value]
      - Panel computed `z-index`: [value]
      - **Verdict:** [confirmed / disproved / partial]

      ## Hypothesis 3 (CSS [hidden] override)
      - Rules overriding `[hidden] { display: none }`: [list, or "none found"]
      - **Verdict:** [confirmed / disproved]

      ## Console errors during click
      - [list, or "none"]

      ## Decision
      - **Root cause:** [one of the three, or "other — see below"]
      - **Fix path:** [exact file(s) + change(s) for Task 2 to apply]
  </what-built>
  <how-to-verify>
    Founder review of `.planning/phases/01-close-in-progress-cleanup/01-03-INVESTIGATION.md`. Confirm:
    1. The investigation actually ran (devtools observations + grep outputs are recorded)
    2. The chosen root cause makes sense given the evidence
    3. The fix path is the smallest possible scope (icon-glyph fix preferred over front-page.php/home.css edits per D-19 + the home-page lock minimization principle)
    4. If the fix path requires editing the locked `front-page.php` or `home.css`, the founder explicitly approves (otherwise the executor reverts to the icon-glyph alias path or escalates)
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/01-close-in-progress-cleanup/01-03-INVESTIGATION.md` exists with the 4 sections (Hypothesis 1, 2, 3, Console errors, Decision) populated
    - Section "Decision → Root cause" is non-empty
    - Section "Decision → Fix path" lists exact file paths + the change to apply (e.g., "add CSS rule `.icon-chevron-down:before { content: '\e9XX'; }` to assets/library/sdwd-icons/style.css")
    - Founder typed "approved" or wrote a corrected fix path
  </acceptance_criteria>
  <resume-signal>Type "approved" with the chosen fix path, or write a corrected fix path. If the fix requires editing front-page.php or home.css, type "approved — home-page lock unlocked for: {specific files + specific changes}".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply the minimal fix per the approved investigation</name>
  <files>wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css (most-likely path per PATTERNS.md hypothesis #1) — actual files determined by Task 1 outcome</files>
  <read_first>
    - .planning/phases/01-close-in-progress-cleanup/01-03-INVESTIGATION.md (the just-confirmed root cause + fix path from Task 1)
    - wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css (current state; check the format of an existing icon-* rule to match the pattern when adding `icon-chevron-down`)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§front-page.php investigation summary — for context if hypothesis 2 or 3 was the actual root cause)
    - CLAUDE.md (re-read §"Locked Pages" if Task 1's approved fix path includes front-page.php or home.css — confirm scope is strictly the dropdown bug)
  </read_first>
  <behavior>
    - The fix applied EXACTLY matches the "Fix path" approved in Task 1 — no extra changes.
    - If hypothesis #1 (most likely): a single CSS rule is added to `assets/library/sdwd-icons/style.css` either (a) defining `.icon-chevron-down:before` with a glyph code that exists in the font, OR (b) aliasing `.icon-chevron-down:before` to use the same `content:` value as the existing `.icon-angle-down:before` (a chevron-down lookalike already in the font). The added rule matches the format of surrounding rules in the file (no `!important`, no inline style).
    - If hypothesis #2: the minimum CSS change in `home.css` to lift the clipping (e.g., remove `overflow: hidden` from a parent, or add `position: relative` + sufficient z-index). Touches only the rule(s) clipping the dropdown.
    - If hypothesis #3: the minimum CSS change to make `[hidden]` re-engage; possibly removing a stray `display: block` rule.
    - If hypothesis "other": the minimum-scope fix in app.js, front-page.php, or home.css per the Task 1 approval.
    - Banned dependencies / styles MUST NOT be introduced (no Bootstrap, no FA, no jQuery, no Google Fonts, no `!important`, no raw hex outside existing patterns, no inline styles).
    - Per D-23: shared tokens in `foundation.css :root`, `components.css`, and `layout.css` are NOT modified.
    - Per D-20: `inc/sd-mega-navwalker.php` is NOT touched.
  </behavior>
  <action>
    Apply the fix per Task 1's approved fix path. The most-likely path (hypothesis #1) playbook:

    **If hypothesis #1 (icon-chevron-down missing) is confirmed**, add a CSS rule to `wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css`:

    Option A — alias to the existing `icon-angle-down` glyph (smallest edit, no font-file change required):
      ```css
      .icon-chevron-down:before {
        content: "\eXXX"; /* same content code as .icon-angle-down — read the existing rule first */
      }
      ```
      Read `.icon-angle-down:before` in the file first to copy its `content:` value verbatim.

    Option B — if the font contains a true chevron-down glyph at a different codepoint that is just missing its CSS class, add:
      ```css
      .icon-chevron-down:before {
        content: "\eYYY"; /* the actual chevron-down codepoint as discovered in Task 1 investigation */
      }
      ```

    Match the formatting of surrounding rules in sdwd-icons/style.css (indentation, quote style, semicolon placement). Do not add comments to existing rules. Do not modify the icomoon font binary files.

    **If hypothesis #2 or #3** is confirmed, follow Task 1's approved Fix path exactly. If that requires editing `front-page.php` or `home.css`, the home-page scoped unlock applies — keep edits strictly within the dropdown bug. If the change feels like it's drifting outside the dropdown, STOP and re-confirm with the founder.

    **Verification commands** (run these after the edit):
      - `grep -c "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css` — expect `>= 1` (rule added)
      - `grep -c "!important" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css` — expect `0` for any newly-added rule (existing `!important`s in the icomoon-generated file are pre-existing; do not introduce new ones)

    Commit message: `fix(01-03): home-page category dropdown — {one-line root cause + fix} (P1-FIX-02)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css || echo "Hypothesis #1 not the chosen path — verifying by Task 1 fix path instead" &amp;&amp; \
      git diff --stat HEAD~1 HEAD -- wp-content/themes/sandiegoweddingdirectory/</automated>
  </verify>
  <acceptance_criteria>
    - The exact files listed in the Task 1 "Fix path" are the ONLY files modified by Task 2 (verify with `git diff --stat`)
    - For hypothesis #1: `grep -c "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/assets/library/sdwd-icons/style.css` returns `>= 1` (the rule was added) AND `grep -c "icon-chevron-down" wp-content/themes/sandiegoweddingdirectory/front-page.php` is unchanged from the pre-fix value (no markup change, only CSS) — UNLESS Task 1 approved a markup-rename approach
    - No new `!important` introduced in any modified file (`git diff HEAD~1 HEAD -- {modified files} | grep '^+' | grep -c '!important'` returns `0`)
    - No banned dependency strings appear in the diff (`git diff HEAD~1 HEAD | grep -ciE 'bootstrap|font-?awesome|jquery|fonts.googleapis'` returns `0`)
    - If front-page.php or home.css was modified, the diff is < 10 lines and is strictly scoped to the dropdown (no other home-page elements touched). If diff exceeds 10 lines OR touches non-dropdown selectors, the task FAILS and must be redone.
    - Runtime check (defer to PLAN 07 verifier): on `http://localhost:8080/`, click the category-search trigger; the dropdown panel opens; vendor categories are listed; clicking one writes the term ID to the hidden `cat_id` input and closes the panel; clicking outside the dropdown closes it.
  </acceptance_criteria>
  <done>The dropdown opens, displays selectable category options, and selection passes through to the hidden `cat_id` input (mirrors ROADMAP.md Phase 1 success criterion #2 and D-21). The fix is the minimum-scope edit to the file(s) approved in Task 1. Atomic commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual browser verification of the dropdown fix; confirm home-page lock can re-engage</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/front-page.php (skim — confirm no unintended changes)
    - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/home.css (skim — confirm no unintended changes if not in fix path)
    - .planning/phases/01-close-in-progress-cleanup/01-03-INVESTIGATION.md
  </read_first>
  <what-built>
    The fix from Task 2. Now verify it actually works in the browser, and confirm the home-page lock can re-engage.
  </what-built>
  <how-to-verify>
    1. **Visit** `http://localhost:8080/` in Chrome (or any local browser pointing at the Docker dev site).
    2. **Locate** the home-page hero search bar; specifically the "Search by category" trigger (it has the chevron-down icon next to it).
    3. **Click** the trigger:
       - The dropdown panel opens BELOW the trigger.
       - The chevron-down icon is visible on the trigger (rendered glyph, not empty space).
       - The panel shows a grid of vendor categories (e.g., "Photographers", "DJs", "Florists", etc. — whatever `get_terms( vendor-category )` returns on the dev site).
    4. **Click** one of the category options:
       - The active state toggles (the clicked option gets `.hero__dropdown-item--active`).
       - The trigger text updates to show the selected category name.
       - The panel closes.
       - Inspecting the form in devtools shows `<input type="hidden" name="cat_id">` with the term ID as its value.
    5. **Click** outside the dropdown (e.g., elsewhere on the hero):
       - Any open dropdown panel closes; aria-expanded returns to "false".
    6. **Confirm no other home-page regression**: scroll the home page, verify hero/cards/footer all render as before.
    7. **Confirm the fix scope** in git: `git log --stat HEAD~3..HEAD` shows the diff is contained to the file(s) approved in Task 1 (and the planning artifact 01-03-INVESTIGATION.md). If front-page.php / home.css were NOT modified, the home-page lock window is already effectively re-engaged.

    **Founder approval signals re-lock:** confirming "approved" tells the executor (and PLAN 07 verifier) that the home-page lock is back in effect. Any future edits to `front-page.php` or `home.css` are again gated on explicit founder approval.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 steps above produce the expected behavior
    - Founder typed "approved" — signals home-page lock re-engages
  </acceptance_criteria>
  <resume-signal>Type "approved" (dropdown works, home-page lock re-engaged) or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
No security-relevant changes — out of scope for threat modeling. P1-FIX-02 is a UI bug fix; the most-likely fix touches an icon-font CSS file (zero attack surface). The dropdown does not handle untrusted input (vendor categories are read from `get_terms()` server-side and emitted with `esc_html()`/`esc_attr()`; the existing markup already does that). Form submission downstream of the dropdown is handled by a different system (the hero search form) and is not modified by this plan.
</threat_model>

<verification>
The hypothesis-verification checkpoint (Task 1) + the manual browser verification checkpoint (Task 3) form the verification surface. The fix is the minimum edit to make the dropdown work; success is binary (it opens and selection passes through, or it doesn't).

Edge case to confirm at PLAN 07 verification time: on the home page, both the vendor-category dropdown AND the venue-type dropdown (if rendered) should behave consistently — the JS handler (app.js:184-242) operates on `.hero__dropdown-trigger` generally, so any other dropdown instance on the home page should also work after the icon-glyph fix.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #2 satisfied: "The home-page category-search mega-menu dropdown fires correctly and displays selectable category options (P1-FIX-02 — scoped home-page unlock closes on completion)"
- D-21 success criterion satisfied (same as the ROADMAP one)
- Investigation completed BEFORE any code change (Task 1 checkpoint)
- Fix is the minimum-scope edit (preferably icon-glyph in sdwd-icons/style.css per PATTERNS.md hypothesis #1)
- Home-page lock re-engages on Task 3 approval (or was never exercised if the fix lived entirely outside front-page.php / home.css)
- D-23 respected: no changes to `foundation.css :root` tokens, `components.css`, or `layout.css`
- D-20 respected: `inc/sd-mega-navwalker.php` not touched
- No banned dependencies introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-in-progress-cleanup/01-03-SUMMARY.md` summarizing:
- The investigation findings (which hypothesis was correct; cite the devtools / grep evidence)
- The exact change applied (file + a few lines of diff)
- Whether the home-page scoped unlock was exercised at all
- Confirmation of manual-browser verification (Task 3 founder approval)
- The home-page lock state at end-of-plan ("re-engaged — front-page.php and home.css are LOCKED again per CLAUDE.md")
- Commit hash for the atomic fix commit
</output>
