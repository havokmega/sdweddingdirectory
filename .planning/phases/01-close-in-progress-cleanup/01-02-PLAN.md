---
phase: 01-close-in-progress-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php
autonomous: true
requirements: [P1-FIX-01]
must_haves:
  truths:
    - "The planning-hero registration form's hidden 'action' input value is `sdwd_register` (not the legacy `sdweddingdirectory_couple_register_form_action`)."
    - "The planning-hero form mints a nonce via `wp_create_nonce( 'sdwd_auth_nonce' )` (not the legacy `sdweddingdirectory_couple_registration_security`)."
    - "The form submits POST inputs whose `name` attributes match `auth.php` exactly: first_name, last_name, email, password, account_type=couple, wedding_date."
    - "Account-type is server-pinned to 'couple' via a hidden input value=\"couple\" — clients cannot tamper to register as vendor/admin."
    - "The client-side JS still splits full-name into first/last and auto-generates the password (silent UX preserved per D-08)."
    - "The JS response handler reads the standard WP `wp_send_json_success` envelope (`payload.success`, `payload.data.message`, `payload.data.redirect`) — matches modals.js:99-112."
    - "When a couple submits valid credentials on /wedding-planning/, a WP_User of role 'couple' is created and the user is redirected to /couple-dashboard/."
  artifacts:
    - path: "wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php"
      provides: "Registration form retargeted to sdwd_register with auth.php-aligned field names + WP-envelope-compatible JS handler"
      contains: "sdwd_register"
  key_links:
    - from: "wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php"
      to: "wp-content/plugins/sdwd-core/includes/auth.php"
      via: "POST to admin-ajax.php with action=sdwd_register and nonce=sdwd_auth_nonce"
      pattern: "sdwd_register"
    - from: "planning-hero.php inline JS submit handler"
      to: "WP wp_send_json_success envelope from auth.php:117-120"
      via: "fetch().then(r => r.json()).then(payload => payload.success / payload.data.message / payload.data.redirect)"
      pattern: "payload\\.success"
---

<objective>
Retarget the planning-hero registration form (template-parts/planning/planning-hero.php:62-235) to the current `sdwd_register` AJAX handler in `sdwd-core/includes/auth.php` (D-09). Currently the form submits to a removed legacy action — every couple sign-up from the planning page returns a WordPress admin-ajax `0` response and no account is created (CONCERNS.md HIGH §"Frontend registration submits to a removed legacy AJAX action"). This plan fixes that.

Per D-08, preserve the silent auto-password-generation UX (Step 1 name + email, Step 2 location + wedding date; full name split client-side; password auto-derived; AJAX submit; auto-login + redirect to /couple-dashboard/). Per D-11 + PATTERNS.md AJAX-envelope finding: fix the response-shape mismatch on the JS side (mirror `modals.js:99-112`), NOT by touching `auth.php` (that would break the modal registration flow which already works).

Per D-10: P1-FIX-01 is not truly complete until LG-04/Phase 5/P5-EMAIL-02 delivers a welcome email carrying password plaintext OR a password-set/reset link. Phase 1 closes the registration-pipe bug; the welcome-email tail is owned by Phase 5. Document this cross-phase tail in the commit message.

Output: `planning-hero.php` with hidden inputs, visible inputs, JS hidden-field population, JS submit handler, and JS response handler all aligned to the current `sdwd_register` contract; sign-ups from /wedding-planning/ create couple accounts and redirect to /couple-dashboard/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/CLAUDE-GSD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-close-in-progress-cleanup/01-CONTEXT.md
@.planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Authoritative contracts from auth.php and modals.js. Extracted from PATTERNS.md §planning-hero.php. -->

`sdwd_register` AJAX handler — field names READ from $_POST (auth.php:23-27, 93-100):
  $account_type = sanitize_text_field( wp_unslash( $_POST['account_type'] ?? '' ) );  // server-pin to "couple" via hidden input
  $first_name   = sanitize_text_field( wp_unslash( $_POST['first_name']   ?? '' ) );
  $last_name    = sanitize_text_field( wp_unslash( $_POST['last_name']    ?? '' ) );
  $email        = sanitize_email(      wp_unslash( $_POST['email']        ?? '' ) );  // ALSO used as user_login
  $password     = $_POST['password'] ?? '';                                            // raw, no unslash (WP convention for passwords)
  // Couple-specific:
  $wedding_date = sanitize_text_field( wp_unslash( $_POST['wedding_date'] ?? '' ) );
  $couple_type  = sanitize_text_field( wp_unslash( $_POST['couple_type']  ?? '' ) );  // optional — current planning form sends "Planning my wedding"

`sdwd_register` nonce verification — auth.php top of handler:
  check_ajax_referer( 'sdwd_auth_nonce', 'nonce' );
  // The form must POST a field named "nonce" (not "security" — the legacy field) whose value
  // is wp_create_nonce( 'sdwd_auth_nonce' ).

`sdwd_register` SUCCESS envelope (auth.php:117-120 — standard WP):
  wp_send_json_success( [
      'message'  => __( 'Account created successfully!', 'sdwd-core' ),
      'redirect' => $redirects[ $account_type ] ?? home_url(),
  ] );
  // Wire-shape: { success: true, data: { message: "...", redirect: "https://.../couple-dashboard/" } }
  // For account_type=couple, the redirect URL points to /couple-dashboard/.

`sdwd_register` ERROR envelope (auth.php — standard WP):
  wp_send_json_error( [ 'message' => '...' ] );
  // Wire-shape: { success: false, data: { message: "..." } }

modals.js:99-112 — canonical envelope-handling pattern (the planning-hero JS must mirror this):
  .then(payload => {
      if (payload && payload.success) {
          // success: read payload.data.message and payload.data.redirect
      } else {
          // error: read payload.data.message
      }
  })

Existing planning-hero.php structure (per PATTERNS.md):
  Lines 62-67  — hidden inputs (action, nonce, redirect_link, first_name, last_name, username, password, register_couple_person)  ← REWRITE
  Lines 79     — visible full-name input (client-side only, JS splits to first/last)                                              ← KEEP name attr or rename to "full_name"
  Line  87     — visible email input                                                                                              ← RENAME name to "email"
  Line  112    — visible location input (client-side only, NOT consumed by auth.php)                                              ← KEEP as-is (UX-only)
  Line  124    — visible wedding-date input                                                                                       ← RENAME name to "wedding_date"
  Line  154    — referenced by current handler (per CONCERNS.md citation)                                                         ← review during edit
  Lines 222-241 — populateHiddenFields() JS — reads/writes legacy hidden field names                                              ← REWRITE
  Lines 316-353 — submit handler with nonce-duplication block + flat-payload reader                                               ← REWRITE
  Line  71     — .planning-hero__form-message banner (KEEP — CSS in planning.css:137-158 unchanged)

Banner CSS (already in place, no change needed):
  .planning-hero__form-message.is-error    { color: var(--sdwd-alert); }
  .planning-hero__form-message.is-success  { color: var(--sdwd-accent-dark); }
  .planning-hero__form-message.is-info     { color: var(--sdwd-muted); }

Cross-phase tail (per D-10) — NOT in this plan's scope, but reference in commit:
  P5-EMAIL-02 (Phase 5) MUST deliver a welcome email containing the auto-generated password plaintext
  OR a password-set/reset link. Without that, a couple who logs out before manually setting their own
  password is locked out. P1-FIX-01 closes the registration pipe; P5-EMAIL-02 closes the recovery loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retarget planning-hero registration form (hidden inputs, visible input names, nonce, action)</name>
  <files>wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php (current state — lines 62-235 per CONCERNS.md HIGH; this is the entire form scope)
    - wp-content/plugins/sdwd-core/includes/auth.php (lines 18-121 — `sdwd_handle_register` function; see <interfaces> for the authoritative field-name list; reads $_POST['first_name'], $_POST['last_name'], $_POST['email'], $_POST['password'], $_POST['account_type'], $_POST['wedding_date'], $_POST['couple_type'])
    - wp-content/themes/sandiegoweddingdirectory/template-parts/modals/couple-registration.php (analog — the canonical modern registration form already targeting sdwd_register; field-name reference)
    - wp-content/themes/sandiegoweddingdirectory/assets/js/modals.js (lines 99-112 — canonical envelope-handling pattern to mirror in Task 2)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§planning-hero.php — full before/after hidden-input + visible-input blocks)
    - .planning/codebase/CONCERNS.md (§"Frontend registration submits to a removed legacy AJAX action" — the bug being fixed)
  </read_first>
  <behavior>
    - The 8 legacy hidden inputs at planning-hero.php:62-67 are replaced with the 7 sdwd_register-aligned hidden inputs (action=sdwd_register; nonce=wp_create_nonce('sdwd_auth_nonce'); account_type=couple [server-pinned, hidden]; first_name; last_name; password; couple_type=planning_wedding).
    - The visible email input (line 87) `name` attribute is renamed from `sdweddingdirectory_couple_register_email` → `email` (auth.php reads $_POST['email']).
    - The visible wedding-date input (line 124) `name` attribute is renamed from `sdweddingdirectory_couple_register_wedding_date` → `wedding_date` (auth.php reads $_POST['wedding_date']).
    - The visible full-name input (line 79) keeps a stable `name` so the JS can read it client-side; PATTERNS.md notes either keep it or rename to `full_name` — pick `full_name` for clarity. This input is NOT sent to the server (it's split client-side into the hidden first_name/last_name fields).
    - The visible location input (line 112) is NOT consumed by auth.php (per PATTERNS.md note); leave its `name` as-is — this is a UX-collection-only field and persisting it is a Phase 2+ decision per D-09.
    - The legacy `username` hidden input is dropped (auth.php uses email as both user_login and user_email — wp_create_user($email, $password, $email)).
    - The legacy `redirect_link` hidden input is dropped (auth.php returns the redirect server-side in the success payload; the JS will read it from there).
    - All output is properly escaped: hidden input `value` attributes use `esc_attr()`, the nonce uses `esc_attr( wp_create_nonce( 'sdwd_auth_nonce' ) )`.
    - The `.planning-hero__form-message` banner element at line 71 is preserved unchanged (Task 2's JS continues to write to it).
    - The ABSPATH guard at the top of the file is preserved.
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php`:

    1. **Replace the 8 legacy hidden inputs** at lines 62-67 (per PATTERNS.md §planning-hero.php — "Hidden input retarget pattern"):
       BEFORE:
       ```php
       <input type="hidden" name="action" value="sdweddingdirectory_couple_register_form_action" />
       <input type="hidden" name="sdweddingdirectory_couple_registration_security" value="<?php echo esc_attr( wp_create_nonce( 'sdweddingdirectory_couple_registration_security' ) ); ?>" />
       <input type="hidden" name="redirect_link" value="<?php echo esc_url( home_url( '/couple-dashboard/' ) ); ?>" />
       <input type="hidden" name="sdweddingdirectory_couple_register_first_name" value="" />
       <input type="hidden" name="sdweddingdirectory_couple_register_last_name" value="" />
       <input type="hidden" name="sdweddingdirectory_couple_register_username" value="" />
       <input type="hidden" name="sdweddingdirectory_couple_register_password" value="" />
       <input type="hidden" name="sdweddingdirectory_register_couple_person" value="Planning my wedding" />
       ```
       AFTER (per D-09 — names match auth.php exactly; account_type pinned server-side via hidden input):
       ```php
       <input type="hidden" name="action" value="sdwd_register" />
       <input type="hidden" name="nonce" value="<?php echo esc_attr( wp_create_nonce( 'sdwd_auth_nonce' ) ); ?>" />
       <input type="hidden" name="account_type" value="couple" />
       <input type="hidden" name="first_name" value="" />
       <input type="hidden" name="last_name" value="" />
       <input type="hidden" name="password" value="" />
       <input type="hidden" name="couple_type" value="planning_wedding" />
       ```

    2. **Rename the visible email input** at line 87 — change `name="sdweddingdirectory_couple_register_email"` → `name="email"`. Do not change `id`, `class`, `placeholder`, `type`, `required`, or any other attribute.

    3. **Rename the visible wedding-date input** at line 124 — change `name="sdweddingdirectory_couple_register_wedding_date"` → `name="wedding_date"`. Do not change other attributes.

    4. **Rename the visible full-name input** at line 79 — change `name="sdweddingdirectory_couple_register_full_name"` → `name="full_name"` (Task 2's JS will read it by this new name to populate the hidden first_name/last_name fields). Do not change other attributes.

    5. **Leave the visible location input** at line 112 unchanged. Per PATTERNS.md / D-09, auth.php does not consume location; persisting it is a Phase 2+ decision. The current `sdweddingdirectory_couple_register_location` name is fine for now.

    6. Preserve the `.planning-hero__form-message` banner at line 71 — Task 2's JS handler continues to write to it.

    7. Preserve `defined( 'ABSPATH' ) || exit;` at the top of the file. Do not add new comments to existing code.

    8. Per D-09 and CLAUDE.md output-escaping rule: confirm every dynamic `value=""` is wrapped in `esc_attr()` and every URL is wrapped in `esc_url()`.

    Commit message: `fix(01-02): retarget planning-hero form to sdwd_register; align field names + nonce (P1-FIX-01) — cross-phase tail: P5-EMAIL-02 must carry welcome-email password material per D-10`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c 'name="action" value="sdwd_register"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "wp_create_nonce( 'sdwd_auth_nonce' )" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="account_type" value="couple"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="first_name"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="last_name"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="email"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="password"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="wedding_date"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c 'name="couple_type"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "sdweddingdirectory_couple_register" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "sdweddingdirectory_couple_registration_security" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name="action" value="sdwd_register"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (action retargeted per D-09)
    - `grep -c "wp_create_nonce( 'sdwd_auth_nonce' )" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (nonce minted with the correct slug)
    - `grep -c 'name="account_type" value="couple"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (account-type server-pinned to couple — anti-tampering)
    - `grep -c 'name="first_name"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (auth.php field name match)
    - `grep -c 'name="last_name"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c 'name="email"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c 'name="password"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c 'name="wedding_date"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c 'name="couple_type"' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c "sdweddingdirectory_couple_register" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (all legacy field-name strings removed)
    - `grep -c "sdweddingdirectory_couple_registration_security" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (legacy nonce slug fully removed)
    - `grep -c "name=\"username\"" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (legacy username hidden input dropped)
    - `grep -c "planning-hero__form-message" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (banner element preserved for Task 2)
    - `grep -c "defined( 'ABSPATH' ) || exit" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (ABSPATH guard preserved)
  </acceptance_criteria>
  <done>The planning-hero form's hidden inputs, the visible email input name, the visible wedding-date input name, and the visible full-name input name all match `auth.php`'s authoritative field list. The legacy action and nonce slugs are fully removed. The `account_type` is server-pinned to `couple` via a hidden input. Commit landed atomically with the cross-phase tail comment for P5-EMAIL-02.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update inline JS — populateHiddenFields(), submit handler, and response handler to match the new contract + WP envelope</name>
  <files>wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php (current state of inline JS — lines 222-241 populateHiddenFields, lines 316-353 submit/response handler; per PATTERNS.md §planning-hero.php "JS adjustments" + "JS submit handler" subsections)
    - wp-content/themes/sandiegoweddingdirectory/assets/js/modals.js (lines 99-112 — canonical envelope-handling pattern to mirror)
    - wp-content/plugins/sdwd-core/includes/auth.php (lines 117-120 — confirm the success envelope shape: wp_send_json_success([ 'message' => ..., 'redirect' => ... ]))
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§planning-hero.php — full before/after JS blocks for both populateHiddenFields and submit handler)
  </read_first>
  <behavior>
    - The `populateHiddenFields()` JS reads the visible full-name input by its new name `full_name`, splits it client-side into first/last, and writes the result to the hidden inputs `first_name` and `last_name` (no longer the legacy `sdweddingdirectory_couple_register_*` field names).
    - The `populateHiddenFields()` JS no longer generates a `username` (auth.php uses email as username — wp_create_user($email, $password, $email) per D-09 / PATTERNS.md).
    - The `populateHiddenFields()` JS still auto-generates the password silently and writes it to the new hidden `password` input (silent UX preserved per D-08; the auto-generated password satisfies auth.php's ≥8-char minimum at auth.php:27, 51-53).
    - The submit handler no longer duplicates the nonce as a `security` field (auth.php expects the nonce in the `nonce` field, which is now set via the hidden input per Task 1).
    - The response handler reads the standard WP `wp_send_json_success` envelope (`payload.success`, `payload.data.message`, `payload.data.redirect`) — mirrors `modals.js:99-112`. On success, it writes `payload.data.message` to `.planning-hero__form-message` with `is-success`, then redirects to `payload.data.redirect` after the existing 350ms delay (preserves D-11 UX).
    - On error, the handler writes `payload.data.message` (or a localized fallback) to `.planning-hero__form-message` with `is-error`.
    - The `.planning-hero__form-message` banner CSS classes (`is-error`, `is-success`, `is-info`) are unchanged — those exist in `planning.css:148-158` per PATTERNS.md.
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` (the inline JS block between roughly line 184 and line 360 — read first to confirm exact line numbers in the current file state):

    1. **Update `populateHiddenFields()` to read the renamed visible input and write the renamed hidden inputs** (per PATTERNS.md §planning-hero.php "JS adjustments — populateHiddenFields() rewrite"):
       BEFORE (current selectors):
       ```javascript
       form.querySelector('[name="sdweddingdirectory_couple_register_first_name"]').value = firstName;
       form.querySelector('[name="sdweddingdirectory_couple_register_last_name"]').value = lastName || firstName;
       // ... + username generation block (DROP this entire block)
       form.querySelector('[name="sdweddingdirectory_couple_register_password"]').value = pass;
       ```
       AFTER (selectors match Task 1 hidden-input rename; username block dropped per PATTERNS.md):
       ```javascript
       form.querySelector('[name="first_name"]').value = firstName;
       form.querySelector('[name="last_name"]').value = lastName || firstName;
       form.querySelector('[name="password"]').value = pass;
       ```
       Also: if the JS reads the visible full-name input by `[name="sdweddingdirectory_couple_register_full_name"]`, update that selector to `[name="full_name"]` (Task 1 renamed it). The auto-password-generation logic (the function that produces `pass`) stays as-is — silent generation is preserved per D-08.

    2. **Update the submit handler — drop the legacy nonce-duplication block** (per PATTERNS.md §planning-hero.php "JS submit handler"). Lines that look like:
       ```javascript
       var nonceValue = formData.get('sdweddingdirectory_couple_registration_security');
       formData.append('security', nonceValue ? nonceValue : '');
       ```
       Should be DELETED. The nonce is now a hidden form field named `nonce` (Task 1 set it), so it's already in `formData` with the right key. No JS-side duplication needed.

    3. **Update the response handler to read the WP envelope** (per PATTERNS.md §planning-hero.php "JS submit handler"):
       BEFORE (current — expects flat `{message, notice, redirect, redirect_link}`):
       ```javascript
       .then(function (payload) {
           if (payload && payload.message) {
               setMessage(payload.message, payload.notice === 1 ? 'is-success' : 'is-error');
           }
           if (payload && payload.redirect === true) {
               var redirectUrl = payload.redirect_link ? payload.redirect_link : '<?php echo esc_url( home_url( '/couple-dashboard/' ) ); ?>';
               window.setTimeout(function () { window.location.href = redirectUrl; }, 350);
           }
       })
       ```
       AFTER (mirrors modals.js:99-112 — reads the standard WP envelope):
       ```javascript
       .then(function (payload) {
           if (payload && payload.success) {
               setMessage((payload.data && payload.data.message) || '', 'is-success');
               if (payload.data && payload.data.redirect) {
                   window.setTimeout(function () { window.location.href = payload.data.redirect; }, 350);
               }
           } else {
               setMessage((payload && payload.data && payload.data.message) || '<?php echo esc_js( __( 'Something went wrong.', 'sandiegoweddingdirectory' ) ); ?>', 'is-error');
           }
       })
       ```

    4. Per CLAUDE.md "What NOT to Do": do not add comments to code you did not change. Do not refactor surrounding JS (event-binding, the `setMessage` helper, the password-generation helper, the step-1/step-2 transition logic). Scope strictly to the three sub-edits above.

    5. The fetch `.catch` handler (network/HTTP errors), if present, can stay as-is — it is independent of the envelope shape.

    Commit message: `fix(01-02): planning-hero JS reads sdwd_register WP envelope; drop legacy nonce duplication (P1-FIX-01)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c "payload.success" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "payload.data.message" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "payload.data.redirect" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "\\[name=\\\"first_name\\\"\\]" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "\\[name=\\\"last_name\\\"\\]" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "\\[name=\\\"password\\\"\\]" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "sdweddingdirectory_couple_register" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "formData.append('security'" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "redirect_link" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php &amp;&amp; \
      grep -c "payload.notice" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "payload.success" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (WP envelope read)
    - `grep -c "payload.data.message" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (envelope.data.message read)
    - `grep -c "payload.data.redirect" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (envelope.data.redirect read)
    - `grep -c '\[name="first_name"\]' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (JS selector matches Task 1 hidden input)
    - `grep -c '\[name="last_name"\]' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c '\[name="password"\]' wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1`
    - `grep -c "sdweddingdirectory_couple_register" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (zero legacy field-name strings remain in markup OR JS)
    - `grep -c "formData.append('security'" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (legacy nonce-duplication block removed)
    - `grep -c "redirect_link" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (legacy hidden input AND legacy `payload.redirect_link` reader both removed; if a `redirect_link` reference remains it indicates the response handler still uses the legacy envelope shape)
    - `grep -c "payload.notice" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `0` (legacy `payload.notice === 1` reader removed)
    - `grep -c "planning-hero__form-message" wp-content/themes/sandiegoweddingdirectory/template-parts/planning/planning-hero.php` returns `>= 1` (banner element preserved)
    - End-to-end runtime check (defer to PLAN 07 verifier; document the steps): on the local Docker dev site, visit /wedding-planning/, fill the hero form with a fresh email, submit, expect (a) `wp_users` table gets a new row with `user_login`=email, `user_email`=email, role meta `couple`; (b) browser redirects to /couple-dashboard/ within ~350ms.
  </acceptance_criteria>
  <done>The inline JS in planning-hero.php (a) reads the WP `wp_send_json_success` envelope, (b) populates hidden fields by their new names, (c) does not duplicate the nonce as `security`, (d) does not reference any legacy `sdweddingdirectory_couple_register_*` selector. A fresh registration submission against the local dev site creates a couple WP_User and redirects to /couple-dashboard/. Commit landed atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin-ajax.php (sdwd_register handler) | Untrusted user input (email, password, names, wedding_date) crosses from public planning page into the auth handler. |
| client-side hidden fields → server | Hidden input values (`account_type=couple`, `couple_type=planning_wedding`, `nonce`, `password`, `first_name`, `last_name`) are client-controlled — anything submitted MUST be re-validated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S (Spoofing) — CSRF | planning-hero registration form → sdwd_register | mitigate | Hidden input `nonce` minted with `wp_create_nonce('sdwd_auth_nonce')` in the form (Task 1); `auth.php:check_ajax_referer('sdwd_auth_nonce', 'nonce')` verifies on the server (existing in auth.php). |
| T-01-02 | T (Tampering) — Account-type elevation | Hidden `account_type` input | mitigate | The hidden input is pinned `value="couple"` in markup (Task 1), but a malicious client could POST `account_type=administrator` directly. **Server-side mitigation is REQUIRED but lives in auth.php — this plan must verify auth.php whitelists `account_type` to {couple, vendor, venue} (auth.php:23-27 sanitizes via `sanitize_text_field`; the redirect array `$redirects[$account_type]` short-circuits unknown types via `?? home_url()`, but role assignment in auth.php must enforce the whitelist).** Confirm during execution by reading auth.php; if no whitelist exists, file a Phase-2 follow-up under P2-SEC-* (do NOT modify auth.php in this plan — out of scope). Disposition: mitigate by current pinning + nonce; documented residual risk → Phase 2 hardening. |
| T-01-03 | I (Information disclosure) — Email enumeration | Error responses on duplicate registration | accept | auth.php returns "email already registered" — standard WP behavior; same surface as wp-login. Out of scope for Phase 1. Logged for completeness. |
| T-01-04 | E (Elevation of privilege) — Auto-generated password lockout | Silent password generation + missing recovery email | mitigate (cross-phase) | The auto-generated password is never shown to the couple; if they log out before manually setting one, they are locked out. **P5-EMAIL-02 (Phase 5) MUST deliver a welcome email containing the password plaintext OR a password-set/reset link.** Documented in commit message (D-10). Phase 1 closes the registration pipe; Phase 5 closes the recovery loop. |
| T-01-05 | T (Tampering) — Input injection | Names, email, wedding_date fields | mitigate | auth.php sanitizes via `sanitize_text_field( wp_unslash( ... ) )` for names, `sanitize_email( wp_unslash( ... ) )` for email, `sanitize_text_field( wp_unslash( ... ) )` for wedding_date. Existing in auth.php — no change needed in this plan. |
| T-01-06 | I (Information disclosure) — Password in transit | Auto-generated password POSTed over HTTP | accept (local dev) / mitigate (production) | Local dev runs on http://localhost:8080 — acceptable for dev. Production will run over HTTPS (standard launch requirement; covered by hosting provider + P5-LAUNCH-01 pre-launch checklist). No code change in this plan. |
| T-01-07 | R (Repudiation) — No registration audit log | sdwd_register handler | accept | Out of Phase 1 scope. WordPress core does not log registrations by default; adding audit trails is a P5-LAUNCH-01 hygiene item if needed. |
| T-01-08 | D (Denial of service) — Rate-limit absence on register | sdwd_register handler | accept (Phase 2) | P2-SEC-04 adds transient-counter rate limiting to login + password-change; registration rate-limiting is not in P2-SEC-* but should be considered if abuse surfaces. Documented; not blocking Phase 1. |

**ASVS L1 alignment:** T-01-01 (CSRF) addressed via nonce; T-01-05 (input validation) addressed via sanitizers; T-01-02 (authorization) partially addressed (server pinning is plan-scope; full role-whitelist enforcement is auth.php's responsibility — verify during execution and file Phase-2 follow-up if missing).

**Cross-phase dependency:** T-01-04's mitigation lives in P5-EMAIL-02 — referenced in commit message per D-10.
</threat_model>

<verification>
After both tasks land:
1. **Static checks (grep)** — covered by the per-task acceptance criteria.
2. **Runtime check (defer to PLAN 07 verifier)** — on the local Docker dev site:
   - Visit `http://localhost:8080/wedding-planning/`
   - Open browser devtools (Network tab)
   - Fill the hero form Step 1 with a unique email like `e2e-test-{timestamp}@example.com` and a name
   - Advance to Step 2, fill location + wedding date, submit
   - Expect: (a) `POST /wp-admin/admin-ajax.php` request with `action=sdwd_register` returns `200` and `{success: true, data: {message: "Account created successfully!", redirect: "http://localhost:8080/couple-dashboard/"}}`; (b) the `.planning-hero__form-message` banner shows the success message; (c) the browser redirects to `/couple-dashboard/` within ~350ms; (d) wp-admin → Users shows the new account with role `couple`.
3. **Negative path:** submit the form a second time with the same email — expect `{success: false, data: {message: "..."}}` and the banner shows the error in red.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #1 satisfied: "A visitor submitting the planning hero registration form successfully creates a couple account (P1-FIX-01 — supports LG-03)"
- All Task 1 + Task 2 acceptance criteria pass via the grep commands listed
- Cross-phase tail captured: commit message references P5-EMAIL-02 + D-10
- No banned dependencies introduced; no other files modified outside `planning-hero.php`
- File boundaries respected: only the theme template-part is modified; `auth.php` (plugin) is NOT touched (per PATTERNS.md AJAX-envelope finding — fixing on JS side has smaller blast radius)
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-in-progress-cleanup/01-02-SUMMARY.md` summarizing:
- The retargeted hidden inputs (action, nonce, account_type, first_name, last_name, password, couple_type) and the renamed visible inputs (email, wedding_date, full_name)
- The JS changes (populateHiddenFields selectors updated; nonce-duplication block removed; response handler now reads the WP envelope)
- Confirmation that auth.php was NOT modified (preserves modal-registration flow)
- The cross-phase tail callout: P5-EMAIL-02 must carry password material (D-10)
- Notes on any auth.php observations (e.g., does the handler whitelist `account_type` to {couple, vendor, venue}? — file a P2-SEC follow-up if not)
- Commit hash(es) for the two atomic commits
</output>
