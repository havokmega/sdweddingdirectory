---
phase: 01-close-in-progress-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - wp-content/themes/sandiegoweddingdirectory/functions.php
  - wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css
autonomous: true
requirements: [P1-CLEAN-01, P1-CLEAN-02, P1-CLEAN-03, P1-CLEAN-05, P1-CLEAN-06]
must_haves:
  truths:
    - "functions.php contains zero references to the dead filter `sdweddingdirectory/post-cap/vendor` (P1-CLEAN-06 — see CONCERNS.md HIGH §'Theme filter hook targets a plugin that no longer exists')."
    - "functions.php contains zero references to the dead filter `sdweddingdirectory_icon_library` (P1-CLEAN-01)."
    - "functions.php contains zero `wp_dequeue_style('sdweddingdirectory-fontello'/'sdweddingdirectory-flaticon')` calls and zero `add_action( 'wp_print_styles', ... )` block invoking those (P1-CLEAN-05)."
    - "functions.php contains zero `add_image_size('sdweddingdirectory_img_*')` calls (P1-CLEAN-02 / per CONCERNS.md MEDIUM §'Theme still has add_image_size entries named for the retired plugin' — these are renamed-or-removed; per D-09 of CONTEXT.md and CONCERNS.md, removal is the cleaner option since no current template fetches those sizes)."
    - "layout.css contains zero `.modal.fade { display: none; }` rules (P1-CLEAN-03)."
    - "All deletes are clean — no orphan comment headers, no broken whitespace structure, no syntax errors."
  artifacts:
    - path: "wp-content/themes/sandiegoweddingdirectory/functions.php"
      provides: "Functions.php with all 4 dead-code blocks removed"
      contains: "load_theme_textdomain"
    - path: "wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css"
      provides: "Layout CSS with the dead .modal.fade rule deleted"
      contains: ".back-to-top"
  key_links:
    - from: "wp-content/themes/sandiegoweddingdirectory/functions.php"
      to: "wp-content/plugins/sdwd-core (via grep)"
      via: "Verify deleted filters have zero consumers in plugin code (already grep-confirmed in CONCERNS.md)"
      pattern: "sdweddingdirectory/post-cap"
---

<objective>
Bundle the theme code-cleanup deletes into one plan because they all touch `functions.php` (and one CSS file) and benefit from atomic per-file commits. Five requirements:

- **P1-CLEAN-01** — Delete dead `sdweddingdirectory_icon_library` filter at `functions.php:245-254` (CONCERNS.md HIGH; no consumer in `sdwd-core`/`sdwd-couple`).
- **P1-CLEAN-02** — Audit `add_image_size('sdweddingdirectory_img_*')` calls at `functions.php:58-59`. Per CONCERNS.md and CONTEXT.md REQUIREMENTS, options are rename to `sdwd_img_*` OR remove if unused. Per CONCERNS.md MEDIUM §"Theme still has add_image_size entries named for the retired plugin": "audit usage; remove or rename to sdwd_*". This plan removes them (no current template fetches those sizes per CONCERNS.md verification — the cleaner option).
- **P1-CLEAN-03** — Delete dead `.modal.fade { display: none; }` rule at `layout.css:687-690` (the rule predates the current modal system and would silently hide future legitimate third-party modals).
- **P1-CLEAN-05** — Delete dead `wp_dequeue_style('sdweddingdirectory-fontello' / '-flaticon')` block at `functions.php:175-181` (handles never enqueued — 4 wasted hook invocations per page load).
- **P1-CLEAN-06** — Delete dead `sdweddingdirectory/post-cap/vendor` filter at `functions.php:240-243` (CONCERNS.md HIGH; vendor CPT in `sdwd-core` uses default capabilities — filter is no-op).

Per CONCERNS.md verification, all four functions.php delete-blocks have ZERO consumers in plugin code (grep-confirmed). These are pure dead-code removals.

Output: `functions.php` and `layout.css` with the 4 functions.php blocks + 1 CSS rule deleted. PHP syntax remains valid; no other behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/CLAUDE-GSD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-close-in-progress-cleanup/01-CONTEXT.md
@.planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Pre-specified line numbers from CONCERNS.md and PATTERNS.md §functions.php. -->

functions.php delete-blocks (line numbers per CONCERNS.md / PATTERNS.md as of 2026-04-23):

  Lines 175-181 (P1-CLEAN-05) — DELETE the entire add_action block:
    ```php
    add_action( 'wp_print_styles', function () {
        wp_dequeue_style( 'sdweddingdirectory-fontello' );
        wp_deregister_style( 'sdweddingdirectory-fontello' );
        wp_dequeue_style( 'sdweddingdirectory-flaticon' );
        wp_deregister_style( 'sdweddingdirectory-flaticon' );
    }, 100 );
    ```

  Lines 240-243 (P1-CLEAN-06) — DELETE the entire add_filter block including its 3-line comment header (lines 239-243 inclusive):
    ```php
    /**
     * Plugin defaults this to true which sets create_posts => do_not_allow.
     * Allow admins to add new vendors from wp-admin.
     */
    add_filter( 'sdweddingdirectory/post-cap/vendor', '__return_false' );
    ```

  Lines 245-254 (P1-CLEAN-01) — DELETE the entire add_filter block including its comment header:
    ```php
    add_filter( 'sdweddingdirectory_icon_library', function ( $args = [] ) {
        // ... function body returning icon set config ...
    } );
    ```

  Lines 57-59 (P1-CLEAN-02) — DELETE the comment header at line 57 + the two add_image_size calls at 58-59:
    ```php
    // Match legacy sizes used by plugins/templates.
    add_image_size( 'sdweddingdirectory_img_600x470', 600, 470, true );
    add_image_size( 'sdweddingdirectory_img_360x480', 360, 480, true );
    ```

layout.css delete-block (per CONCERNS.md MEDIUM §"Bootstrap-modal hide rule without a corresponding modal system"):

  Lines 687-690 (P1-CLEAN-03) — DELETE the comment + rule + braces (4 lines):
    ```css
    /* Hide plugin-injected Bootstrap modals until v2 modal system is built */
    .modal.fade {
      display: none;
    }
    ```
  After delete: confirm the surrounding `.back-to-top` rule at line 692 stays intact and the file's blank-line structure remains clean.

CONCERNS.md verification (already performed by code-mapper, per the audit text):
  - `grep -rn "sdweddingdirectory/post-cap" wp-content/plugins/sdwd-core wp-content/plugins/sdwd-couple` returns zero matches
  - `grep -rn "sdweddingdirectory_icon_library" wp-content/plugins/sdwd-core wp-content/plugins/sdwd-couple` returns zero matches (no plugin code consumes the filter)
  - `wp_dequeue_style('sdweddingdirectory-fontello'/'sdweddingdirectory-flaticon')` — handles never registered; calls are no-op

Sequencing note (file-conflict with PLAN 05):
  Both this plan AND PLAN 05 (text-domain sweep) modify functions.php. They are sequenced via `depends_on` on PLAN 05 (PLAN 05's frontmatter lists this plan as a dependency). This plan runs FIRST so the deletes happen before the text-domain sweep — the swept domain strings inside the to-be-deleted blocks then never need to be touched. This avoids spurious diff churn in the sweep.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the 4 dead-code blocks from functions.php (P1-CLEAN-01, -02, -05, -06)</name>
  <files>wp-content/themes/sandiegoweddingdirectory/functions.php</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/functions.php (current state — confirm exact line numbers; CONCERNS.md was dated 2026-04-23, line numbers should still match)
    - .planning/codebase/CONCERNS.md (HIGH §"Theme filter hook targets a plugin that no longer exists" + HIGH §"wp_print_styles dequeues two stylesheet handles that are never enqueued" + MEDIUM §"Theme still has add_image_size entries named for the retired plugin"; these are the authoritative bug reports)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§functions.php — table mapping line numbers to actions for each REQ)
    - wp-content/plugins/sdwd-core/includes/post-types.php (briefly skim — confirm vendor CPT registration uses default capabilities + map_meta_cap => true so the deleted post-cap filter is genuinely unnecessary)
  </read_first>
  <behavior>
    - The four blocks listed in <interfaces> are removed from `functions.php` cleanly: no orphan comment headers, no double blank lines, no syntax errors.
    - Specifically:
      1. Lines 57-59: comment + 2 add_image_size calls REMOVED.
      2. Lines 175-181: entire add_action('wp_print_styles', ...) block REMOVED (function wrapper + all 4 dequeue/deregister calls + closing `}, 100 );`).
      3. Lines 239-243 (or 240-243 if no preceding comment): add_filter('sdweddingdirectory/post-cap/vendor', '__return_false') call + its 3-line preceding comment REMOVED.
      4. Lines 245-254: add_filter('sdweddingdirectory_icon_library', function (...) { ... }) block REMOVED including any preceding comment.
    - All other code in functions.php is preserved unchanged. The text-domain sweep (PLAN 05) will handle the remaining `sdweddingdirectory-v2` strings in this file later — do NOT do any text-domain work here.
    - PHP syntax remains valid (`php -l` succeeds).
    - The ABSPATH guard at the top of the file is preserved.
    - No new comments are added to surrounding code (CLAUDE.md "What NOT to Do").
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/functions.php` and remove the four blocks listed in <interfaces>. Read the file first to confirm exact current line numbers (CONCERNS.md is dated 2026-04-23; if line numbers have drifted slightly, locate by content not by line number).

    Use the Edit tool 4 times — one delete per block — so each removal is isolated. Order of edits is bottom-up (delete the LAST block first so earlier line numbers don't shift mid-edit):

    1. **Delete the `sdweddingdirectory_icon_library` filter block** (P1-CLEAN-01; was lines 245-254). Find by content: search for the literal string `add_filter( 'sdweddingdirectory_icon_library'`. Delete from the start of any preceding comment header through the closing `} );` of the filter, inclusive. Leave one blank line after the deletion to preserve file readability.

    2. **Delete the `sdweddingdirectory/post-cap/vendor` filter block** (P1-CLEAN-06; was lines 239/240-243). Find by content: search for `add_filter( 'sdweddingdirectory/post-cap/vendor'`. Delete from the start of the preceding 3-line comment block (`/** ... Allow admins to add new vendors from wp-admin. */`) through the `add_filter(...)` line inclusive.

    3. **Delete the `wp_print_styles` dequeue block** (P1-CLEAN-05; was lines 175-181). Find by content: search for `add_action( 'wp_print_styles'` followed by a function that calls `wp_dequeue_style( 'sdweddingdirectory-fontello'` (this disambiguates from any other wp_print_styles hook in the file). Delete from `add_action( 'wp_print_styles', function () {` through the closing `}, 100 );` inclusive. Also delete any single-line comment header immediately above (e.g., a `// Dequeue legacy fontello/flaticon styles.` line) if present.

    4. **Delete the legacy `add_image_size` block** (P1-CLEAN-02; was lines 57-59). Find by content: search for `add_image_size( 'sdweddingdirectory_img_600x470'`. Delete that line, the next `add_image_size( 'sdweddingdirectory_img_360x480'` line, AND the preceding comment line (`// Match legacy sizes used by plugins/templates.`).

    After all 4 deletes, confirm with `php -l wp-content/themes/sandiegoweddingdirectory/functions.php` that PHP syntax is valid.

    Commit message: `chore(01-04): remove dead filters, dequeue, image sizes from functions.php (P1-CLEAN-01, -02, -05, -06)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c "sdweddingdirectory_icon_library" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "sdweddingdirectory/post-cap/vendor" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "sdweddingdirectory-fontello" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "sdweddingdirectory-flaticon" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "sdweddingdirectory_img_" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "load_theme_textdomain" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      grep -c "defined( 'ABSPATH' ) || exit" wp-content/themes/sandiegoweddingdirectory/functions.php &amp;&amp; \
      php -l wp-content/themes/sandiegoweddingdirectory/functions.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sdweddingdirectory_icon_library" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (P1-CLEAN-01 satisfied)
    - `grep -c "sdweddingdirectory/post-cap/vendor" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (P1-CLEAN-06 satisfied)
    - `grep -c "sdweddingdirectory-fontello" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (P1-CLEAN-05 satisfied)
    - `grep -c "sdweddingdirectory-flaticon" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (P1-CLEAN-05 satisfied)
    - `grep -c "sdweddingdirectory_img_" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (P1-CLEAN-02 satisfied — both legacy image sizes removed)
    - `grep -c "add_image_size" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `0` (no replacement `sdwd_img_*` calls added — per CONCERNS.md verification, no template fetches those sizes; per D-CLEAN-02 wording in REQUIREMENTS.md, "or removed if unused" applies)
    - `grep -c "load_theme_textdomain" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `>= 1` (the rest of functions.php was NOT damaged — load_theme_textdomain is still present, awaits PLAN 05 sweep for the domain string)
    - `grep -c "defined( 'ABSPATH' ) || exit" wp-content/themes/sandiegoweddingdirectory/functions.php` returns `>= 1` (ABSPATH guard preserved)
    - `php -l wp-content/themes/sandiegoweddingdirectory/functions.php` exits with status 0 and prints `No syntax errors detected` (PHP parse valid)
    - File line count decreased by approximately 25-30 lines (4 deleted blocks total: 3 + 7 + 5 + 10 ≈ 25 lines + comment headers)
  </acceptance_criteria>
  <done>The four dead-code blocks are removed from functions.php. PHP syntax is valid. No other behavior changes. Commit landed atomically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the dead .modal.fade rule from layout.css (P1-CLEAN-03)</name>
  <files>wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css (lines 685-695 — confirm the .modal.fade block at ~687-690 + the surrounding .back-to-top rule at ~692)
    - .planning/codebase/CONCERNS.md (MEDIUM §"Bootstrap-modal hide rule without a corresponding modal system" — the bug report)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§"layout.css §687-690" — full delete block)
  </read_first>
  <behavior>
    - The entire 4-line block (preceding comment + selector + braces) at layout.css:687-690 is removed.
    - The surrounding `.back-to-top` rule at line ~692 is preserved.
    - No other rules in `layout.css` are modified.
    - File CSS syntax remains valid (no orphan braces, no broken selectors).
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css`. Find by content the block:
    ```css
    /* Hide plugin-injected Bootstrap modals until v2 modal system is built */
    .modal.fade {
      display: none;
    }
    ```
    Delete those 4 lines (the comment, the selector + opening brace, the rule, the closing brace). Leave one blank line after the deletion to preserve file readability and ensure the next selector (`.back-to-top` per PATTERNS.md) is properly separated.

    Do not modify any other rule in `layout.css`. Do not add new comments.

    Commit message: `style(01-04): remove dead .modal.fade rule (P1-CLEAN-03)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c "\.modal\.fade" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css &amp;&amp; \
      grep -c "Hide plugin-injected Bootstrap modals" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css &amp;&amp; \
      grep -c "\.back-to-top" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.modal\.fade" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css` returns `0` (selector deleted)
    - `grep -c "Hide plugin-injected Bootstrap modals" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css` returns `0` (preceding comment deleted)
    - `grep -c "\.back-to-top" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css` returns `>= 1` (surrounding rule preserved)
    - File line count decreased by exactly 4 (or 5 if there was a trailing blank line in the deleted block)
    - No `!important` introduced (`grep -c "!important" wp-content/themes/sandiegoweddingdirectory/assets/css/layout.css` is unchanged from pre-edit value — pre-existing `!important`s in layout.css are not touched, but this task introduces zero new ones)
  </acceptance_criteria>
  <done>The dead `.modal.fade` rule is removed from layout.css. The surrounding `.back-to-top` rule is intact. Commit landed atomically.</done>
</task>

</tasks>

<threat_model>
No security-relevant changes — out of scope for threat modeling. P1-CLEAN-01/-02/-03/-05/-06 are all dead-code removals with no input/output, no auth, no user-controlled data flow, no boundary crossings. CONCERNS.md verified zero consumers exist for the deleted filters and zero registrations exist for the dequeued stylesheet handles before this plan runs.
</threat_model>

<verification>
After both tasks land:
- All grep checks above return the expected counts.
- `php -l functions.php` confirms PHP syntax is valid.
- No regression possible — the deleted code was already a no-op (filters with no consumers, dequeues with no registrations, image sizes never fetched, CSS rule with no matching elements in the current modal system).
- PLAN 07 verifier eyeballs functions.php to confirm no orphan comments/blank-line damage.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #4 satisfied: "`functions.php` contains no references to dead legacy filter hooks or dequeue handles for handles that were never enqueued"
- All 5 REQ-IDs (P1-CLEAN-01, -02, -03, -05, -06) closed
- PHP and CSS syntax valid in both modified files
- No banned dependencies introduced
- File boundaries respected (only theme files touched; no plugin files; no `inc/` files; no home-page-locked files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-in-progress-cleanup/01-04-SUMMARY.md` summarizing:
- The 4 functions.php blocks deleted (REQ-IDs + ~line ranges)
- The layout.css rule deleted (REQ-ID + ~line range)
- Confirmation that `php -l functions.php` passes
- Notes on the P1-CLEAN-02 decision (removed rather than renamed because no current template fetches those sizes per CONCERNS.md verification)
- Commit hashes for the two atomic commits
</output>
