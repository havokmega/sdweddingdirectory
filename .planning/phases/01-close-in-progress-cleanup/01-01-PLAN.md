---
phase: 01-close-in-progress-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wp-content/themes/sandiegoweddingdirectory/404.php
  - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css
autonomous: true
requirements: [P1-BUILD-02]
must_haves:
  truths:
    - "The 404 page displays the real SVG asset (404_error.svg), not the placeholder inline circle+404 text."
    - "The 404 page button row contains exactly 5 buttons, all styled with the .btn--outline modifier (no .btn--primary)."
    - "The 404 button row is right-aligned (justify-content: flex-end), not left- or center-aligned."
    - "The 404 button-row inter-button gap uses a foundation.css :root token (no hardcoded px value)."
    - "All 5 button hrefs target the correct destinations via home_url(): /, /wedding-planning/, /venues/, /vendors/, /real-weddings/."
    - "The CSS BEM class names in 404.php markup match the .error-page__* block already defined in static.css (no .error-404__* / .error-page__* parity bug)."
  artifacts:
    - path: "wp-content/themes/sandiegoweddingdirectory/404.php"
      provides: "404 template with real SVG, 5 .btn--outline buttons, .error-page__* BEM markup"
      contains: "404_error.svg"
    - path: "wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css"
      provides: "Token-based gap + flex-end alignment for .error-page__buttons"
      contains: "var(--sdwd-section-gap-sm)"
  key_links:
    - from: "wp-content/themes/sandiegoweddingdirectory/404.php"
      to: "wp-content/themes/sandiegoweddingdirectory/assets/images/404-error-page/404_error.svg"
      via: "<img src=\"<?php echo esc_url( get_template_directory_uri() . '/assets/images/404-error-page/404_error.svg' ); ?>\">"
      pattern: "404_error\\.svg"
    - from: "wp-content/themes/sandiegoweddingdirectory/404.php"
      to: "wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css"
      via: "Conditional enqueue at functions.php:138-141 when is_404()"
      pattern: "\\.error-page__"
---

<objective>
Finish the 404 page touch-ups (P1-BUILD-02). 95% of the page is already built; this plan delivers the final 5%: swap the placeholder inline SVG for the real `404_error.svg` asset (D-03), normalize the button row to all `.btn--outline` (no `.btn--primary` outlier per D-05), right-align the button row with a token-based gap (D-06), and resolve the markup-vs-CSS BEM class-name parity bug surfaced by the pattern mapper (markup currently uses `.error-404__*` but `static.css` defines `.error-page__*` — rename markup to match existing CSS).

Purpose: closes P1-BUILD-02 from REQUIREMENTS.md and satisfies ROADMAP.md Phase 1 success criterion #3 ("The 404 page renders the real SVG asset with a right-aligned button row of all-`.btn--outline` buttons — no `btn--primary` outlier").

Output: Updated `404.php` and `static.css` with the markup, asset reference, button styling, alignment, and BEM class names all matching house style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/CLAUDE-GSD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-close-in-progress-cleanup/01-CONTEXT.md
@.planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Key existing values the executor needs without exploring the codebase. Extracted from PATTERNS.md. -->

Real SVG asset (verified to exist; 4 KiB; dated 2026-04-06):
  wp-content/themes/sandiegoweddingdirectory/assets/images/404-error-page/404_error.svg

Existing CSS BEM block already in static.css:117-143 (the markup must align to THIS):
  .error-page__buttons   (currently centered, hardcoded 12px gap — to be updated)
  .error-page__graphic
  .error-page__title
  .error-page__desc

Current (wrong) markup BEM block in 404.php (must be renamed to .error-page__*):
  .error-404__nav
  .error-404__title
  .error-404__desc
  .error-404__graphic
  .error-404__svg

Conditional CSS enqueue (already wired — DO NOT modify functions.php for this plan):
  functions.php:138-141 — is_404() loads sdwdv2-static (static.css). No enqueue change needed.

Spacing tokens available in foundation.css :root (use these, never hardcode):
  --sdwd-section-gap-sm: 16px       <- recommended for inter-button gap
  --sdwd-section-gap-md: 24px
  --sdwd-section-gap-lg: 48px
  --sdwd-row-gap: 32px              <- recommended for margin-top above the button row
  --sdwd-title-gap: 24px
  --gutter: 20px
  --sdwd-radius / --sdwd-radius-lg

Asset URL pattern used uniformly across the theme:
  <?php echo esc_url( get_template_directory_uri() . '/assets/images/...' ); ?>

Button URLs (per D-05, in left-to-right order):
  Home           -> home_url( '/' )
  Planning Tools -> home_url( '/wedding-planning/' )
  Venues         -> home_url( '/venues/' )
  Vendors        -> home_url( '/vendors/' )
  Real Weddings  -> home_url( '/real-weddings/' )

Text-domain to use in __()/esc_html_e()/esc_attr_e() calls inside 404.php:
  'sandiegoweddingdirectory'  (per D-04 + the P1-CLEAN-04 sweep — 404.php is one of the swept files;
                               write new strings with the correct domain so the sweep is consistent.)

Pattern-mapper finding (PATTERNS.md §Pattern Assignments):
  static.css ALREADY has the right house-style block as .error-page__*; the cheaper fix
  is to rename markup in 404.php — DO NOT add parallel .error-404__* rules to static.css.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace placeholder SVG in 404.php with the real asset and rename markup BEM block to .error-page__*</name>
  <files>wp-content/themes/sandiegoweddingdirectory/404.php</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/404.php (current state — see exact placeholder block at lines 11-16 per PATTERNS.md §404.php)
    - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css (see lines 117-143 — confirms the existing .error-page__* BEM block this markup must align to)
    - wp-content/themes/sandiegoweddingdirectory/front-page.php (lines 310, 315, 363 — analog asset-include pattern: <img loading="lazy" decoding="async" src="<?php echo esc_url( get_template_directory_uri() . '/assets/images/...' ); ?>" alt="..." width=... height=...>)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§404.php — full before/after blocks)
  </read_first>
  <behavior>
    - The placeholder inline SVG (`<svg ... viewBox="0 0 200 160">` with the 404 text and empty circle) is replaced with an `<img>` referencing the real `404_error.svg` asset via `get_template_directory_uri()`.
    - All `.error-404__*` class names in the 404.php markup are renamed to `.error-page__*` to align with the existing CSS in `static.css` (per D-06 implementation note + PATTERNS.md class-name parity finding).
    - The graphic `<img>` includes `loading="lazy"` and `decoding="async"` to match the home-page image-include pattern (front-page.php:310, 315, 363).
    - The 5 button-row anchors are present in the order Home → Planning Tools → Venues → Vendors → Real Weddings (per D-05); each is `<a class="btn btn--outline">` (NO `.btn--primary`); each `href` is wrapped in `esc_url( home_url( '/...' ) )`; each label uses `esc_html_e( '...', 'sandiegoweddingdirectory' )`.
    - The container element (`<nav>` per recommended markup) wraps the 5 anchors and has class `error-page__buttons` and an `aria-label` for accessibility.
    - `<h1>Page Not Found</h1>` text and the description copy are kept as-is per D-04 (only text-domain on translation calls is updated to `'sandiegoweddingdirectory'` so the P1-CLEAN-04 sweep stays consistent).
    - The ABSPATH guard at the top of 404.php is preserved.
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/404.php`:

    1. **Replace the placeholder inline SVG block** (currently at 404.php:11-16 per PATTERNS.md):
       BEFORE:
       ```php
       <div class="error-404__graphic">
           <svg class="error-404__svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 160" width="200" height="160" aria-hidden="true">
               <circle cx="100" cy="80" r="70" fill="none" stroke="currentColor" stroke-width="2" opacity="0.2"/>
               <text x="100" y="95" text-anchor="middle" font-size="48" font-weight="700" fill="currentColor" opacity="0.3">404</text>
           </svg>
       </div>
       ```
       AFTER (per D-03 — uses the real asset; mirrors the front-page.php asset-include pattern):
       ```php
       <div class="error-page__graphic">
           <img
               src="<?php echo esc_url( get_template_directory_uri() . '/assets/images/404-error-page/404_error.svg' ); ?>"
               alt=""
               width="280"
               height="200"
               loading="lazy"
               decoding="async"
           >
       </div>
       ```

    2. **Rename every `.error-404__*` class to `.error-page__*`** elsewhere in 404.php (per PATTERNS.md class-name parity finding — `static.css` already has the `.error-page__*` rules, so the markup must align). Specifically rename:
       - `.error-404__title` → `.error-page__title`
       - `.error-404__desc` → `.error-page__desc`
       - `.error-404__nav` → `.error-page__buttons` (use the recommended container name from PATTERNS.md final markup; this matches the existing CSS selector)
       - Any other `.error-404__*` occurrence → corresponding `.error-page__*`

    3. **Replace the 5-button row** with this exact markup (per D-05 + PATTERNS.md final markup; ALL 5 buttons use `.btn--outline` — NO `.btn--primary`):
       ```php
       <nav class="error-page__buttons" aria-label="<?php esc_attr_e( 'Helpful links', 'sandiegoweddingdirectory' ); ?>">
           <a class="btn btn--outline" href="<?php echo esc_url( home_url( '/' ) ); ?>"><?php esc_html_e( 'Home', 'sandiegoweddingdirectory' ); ?></a>
           <a class="btn btn--outline" href="<?php echo esc_url( home_url( '/wedding-planning/' ) ); ?>"><?php esc_html_e( 'Planning Tools', 'sandiegoweddingdirectory' ); ?></a>
           <a class="btn btn--outline" href="<?php echo esc_url( home_url( '/venues/' ) ); ?>"><?php esc_html_e( 'Venues', 'sandiegoweddingdirectory' ); ?></a>
           <a class="btn btn--outline" href="<?php echo esc_url( home_url( '/vendors/' ) ); ?>"><?php esc_html_e( 'Vendors', 'sandiegoweddingdirectory' ); ?></a>
           <a class="btn btn--outline" href="<?php echo esc_url( home_url( '/real-weddings/' ) ); ?>"><?php esc_html_e( 'Real Weddings', 'sandiegoweddingdirectory' ); ?></a>
       </nav>
       ```

    4. Per D-04, keep the existing `<h1>Page Not Found</h1>` text and the existing description copy. Only update the text-domain on translation calls to `'sandiegoweddingdirectory'` (so this file's strings are consistent with the P1-CLEAN-04 sweep that PLAN 05 will run on the rest of the theme).

    5. Preserve the `defined( 'ABSPATH' ) || exit;` guard at the top of the file. Do not add new comments to existing code per CLAUDE.md "What NOT to Do".

    Commit message: `fix(01-01): finish 404 page — real SVG, all-outline button row, .error-page BEM rename (P1-BUILD-02)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -c "404_error\.svg" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "btn--primary" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "btn--outline" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "error-404__" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "error-page__" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "home_url( '/' )" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "home_url( '/wedding-planning/' )" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "home_url( '/venues/' )" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "home_url( '/vendors/' )" wp-content/themes/sandiegoweddingdirectory/404.php &amp;&amp; \
      grep -c "home_url( '/real-weddings/' )" wp-content/themes/sandiegoweddingdirectory/404.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "404_error\.svg" wp-content/themes/sandiegoweddingdirectory/404.php` returns a value `>= 1` (real asset referenced)
    - `grep -c "btn--primary" wp-content/themes/sandiegoweddingdirectory/404.php` returns `0` (no .btn--primary anywhere)
    - `grep -c "btn--outline" wp-content/themes/sandiegoweddingdirectory/404.php` returns `5` (exactly 5 buttons all .btn--outline)
    - `grep -c "error-404__" wp-content/themes/sandiegoweddingdirectory/404.php` returns `0` (old BEM block fully renamed)
    - `grep -c "error-page__" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 4` (graphic, title, desc, buttons all renamed)
    - `grep -c "home_url( '/' )" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1`
    - `grep -c "home_url( '/wedding-planning/' )" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1`
    - `grep -c "home_url( '/venues/' )" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1`
    - `grep -c "home_url( '/vendors/' )" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1`
    - `grep -c "home_url( '/real-weddings/' )" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1`
    - `grep -E "viewBox=\"0 0 200 160\"" wp-content/themes/sandiegoweddingdirectory/404.php` returns `0` matches (placeholder SVG removed)
    - `grep -c "defined( 'ABSPATH' ) || exit" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1` (ABSPATH guard preserved)
    - `grep -c "loading=\"lazy\"" wp-content/themes/sandiegoweddingdirectory/404.php` returns `>= 1` (lazy-loading attribute on the SVG `<img>`)
  </acceptance_criteria>
  <done>The 404.php template references the real SVG asset, contains exactly 5 `.btn--outline` buttons in the correct order with the correct `home_url()` targets, uses `.error-page__*` BEM markup that matches the existing CSS, and preserves the ABSPATH guard. Commit landed atomically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .error-page__buttons rule in static.css to right-align with a token-based gap</name>
  <files>wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css</files>
  <read_first>
    - wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css (current state — see existing `.error-page__buttons` block around lines 138-143 per PATTERNS.md §static.css)
    - wp-content/themes/sandiegoweddingdirectory/assets/css/foundation.css (verify the spacing tokens listed in <interfaces> exist in `:root` — `--sdwd-section-gap-sm`, `--sdwd-row-gap`)
    - .planning/phases/01-close-in-progress-cleanup/01-PATTERNS.md (§static.css — full before/after CSS block)
  </read_first>
  <behavior>
    - The existing `.error-page__buttons` rule in `static.css` is updated so the button row is right-aligned (`justify-content: flex-end` per D-06) and uses `var(--sdwd-section-gap-sm)` for the inter-button gap (token-based per D-06; no hardcoded `12px` value).
    - A `margin-top: var(--sdwd-row-gap)` is added to space the button row away from the description copy above it (token-based).
    - `display: flex` and `flex-wrap: wrap` are preserved so mobile layouts still wrap gracefully.
    - No raw hex colors, no `!important`, no inline styles introduced (per CLAUDE.md).
    - No other CSS rules in `static.css` are modified — this plan is scoped strictly to `.error-page__buttons`.
  </behavior>
  <action>
    Edit `wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css`:

    Locate the existing `.error-page__buttons { ... }` rule (per PATTERNS.md, lines ~138-143):
    BEFORE:
    ```css
    .error-page__buttons {
      display: flex;
      gap: 12px;
      justify-content: center;
      flex-wrap: wrap;
    }
    ```

    AFTER (per D-06 — right-aligned with token-based gap; PATTERNS.md confirms `var(--sdwd-section-gap-sm)` (16px) is the right token for an inter-button gap and `var(--sdwd-row-gap)` (32px) is right for the margin above the button row):
    ```css
    .error-page__buttons {
      display: flex;
      gap: var(--sdwd-section-gap-sm);
      justify-content: flex-end;
      flex-wrap: wrap;
      margin-top: var(--sdwd-row-gap);
    }
    ```

    Do not modify any other rule in `static.css` (other static-page styles, unrelated `.error-page__*` rules like `.error-page__title` / `.error-page__desc` / `.error-page__graphic`, etc.). Per CLAUDE.md: do not add comments to code you did not change.

    Commit message: `style(01-01): right-align 404 button row with token-based gap (P1-BUILD-02)`
  </action>
  <verify>
    <automated>cd /Users/Havok/Documents/Development/WebDevelopment/wp-docker &amp;&amp; \
      grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -c "justify-content: flex-end" &amp;&amp; \
      grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -c "var(--sdwd-section-gap-sm)" &amp;&amp; \
      grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -cE "gap:\s*12px" &amp;&amp; \
      grep -c "!important" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -c "justify-content: flex-end"` returns `>= 1` (right-aligned per D-06)
    - `grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -c "var(--sdwd-section-gap-sm)"` returns `>= 1` (token-based gap per D-06)
    - `grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -cE "gap:\s*12px"` returns `0` (hardcoded `12px` removed)
    - `grep -A6 "\.error-page__buttons" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css | grep -c "var(--sdwd-row-gap)"` returns `>= 1` (margin-top above the button row uses a token)
    - `grep -c "!important" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css` returns `0` (no `!important` introduced — banned per CLAUDE.md)
    - `grep -E "#[0-9a-fA-F]{3,6}\b" wp-content/themes/sandiegoweddingdirectory/assets/css/pages/static.css` shows no NEW raw hex (the rule should be reviewed; raw hex anywhere in static.css is a pre-existing concern, but this task MUST NOT introduce new ones)
  </acceptance_criteria>
  <done>The `.error-page__buttons` rule in `static.css` is right-aligned (`justify-content: flex-end`), uses `var(--sdwd-section-gap-sm)` for the inter-button gap, and has `margin-top: var(--sdwd-row-gap)` for spacing above the row. No `!important`, no new raw hex, no other rules touched. Commit landed atomically.</done>
</task>

</tasks>

<threat_model>
No security-relevant changes — out of scope for threat modeling. P1-BUILD-02 only edits static front-end markup and CSS; no input/output, no auth, no user-controlled data crosses any boundary.
</threat_model>

<verification>
After both tasks land, the 404 page when rendered (visiting any non-existent URL on the local dev site) must show:
1. The real SVG asset (a designed 404 graphic — not a flat placeholder circle with "404" text)
2. The existing `<h1>Page Not Found</h1>` heading and description copy unchanged
3. A row of 5 buttons, all visually identical (all `.btn--outline`), aligned to the right edge of the content area, with even token-based spacing between them
4. The buttons in order Home → Planning Tools → Venues → Vendors → Real Weddings
5. Each button navigating to its correct destination on click

The verification plan (PLAN 07) will eyeball-confirm this in the browser.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #3 satisfied: "The 404 page renders the real SVG asset with a right-aligned button row of all-`.btn--outline` buttons — no `btn--primary` outlier"
- Both Task 1 and Task 2 acceptance criteria pass via the grep commands listed
- No banned dependencies introduced (no Bootstrap, no FA, no jQuery, no Google Fonts, no `!important`, no raw hex, no inline styles)
- File boundaries respected (only `404.php` and `static.css` modified — not `functions.php`, not `inc/`, not the home-page-locked files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-close-in-progress-cleanup/01-01-SUMMARY.md` summarizing:
- What changed in `404.php` (BEM rename, SVG swap, button-row markup)
- What changed in `static.css` (right-align + token gap + margin-top)
- Confirmation that the placeholder SVG is gone, all 5 buttons are `.btn--outline`, and the markup-vs-CSS class-name parity bug is resolved
- Commit hash(es) for the two atomic commits
</output>
